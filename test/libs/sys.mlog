# =================================================================
# MLC RUNTIME / SYSTEM ABI (sys.mlog)
# =================================================================
# Псевдорегистры общего назначения:
# a0, a1, a2 - Аргументы функций
# v0         - Возвращаемое значение
# ra         - Адрес возврата (метка строки)
# =================================================================